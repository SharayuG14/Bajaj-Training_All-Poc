import { EventsCardList } from './events-card-list';

describe('EventsCardList', () => {
  it('should create an instance', () => {
    const directive = new EventsCardList();
    expect(directive).toBeTruthy();
  });
});
